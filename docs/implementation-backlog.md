# Implementation Backlog

Date: 2026-07-20

This backlog is based on the current-state audit and avoids runtime code changes until source-of-truth documents and production inventory are complete.

## Backlog

| ID | Classification | Scope | Dependency | Risk | Acceptance criteria | Tests required | Affects production data? |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B-001 | `add` | Create `docs/inventory-channel-orchestration.md` defining order channels, capacity states, holds, releases, Square payment reconciliation, waitlist movement, and source-of-truth ownership. | Live inventory of current Sheets, Square webhook destination, and fulfillment rules. | High: wrong orchestration can oversell or drop orders. | Document reviewed by owner; all channels and states mapped; migration rules include rollback and manual override paths. | Documentation review; simulated state-transition table examples. | No, documentation only. |
| B-002 | `add` | Create `docs/data-model.md` with canonical Sheets tabs, columns, status values, derived tabs, D1 tables, PII fields, and append-only migration rules. | Export of live workbook headers and current Apps Script schema. | High: schema drift can break order capture, payments, analytics, or email. | Every current tab has owner, purpose, columns, allowed status values, and migration policy; Apps Script and Worker storage are both covered. | Compare documented columns to `setupSheet`, subscription setup, line-item setup, waitlist setup, email log setup, and Worker D1 migration/config. | No, documentation only. |
| B-003 | `add` | Create `docs/ai-content-governance.md` covering AI-generated copy, newsletter campaigns, customer emails, human approval, PII handling, prohibited claims, and publish/send gates. | Human approval policy and brand/content owner review. | Medium: unsafe or inaccurate customer-facing content could be sent or published. | Clear approval matrix for website copy, transactional email, newsletter sends, social copy, and generated images; emergency rollback guidance included. | Documentation review; checklist dry run for a newsletter and website-copy change. | No, documentation only. |
| B-004 | `unknown` | Inventory production Apps Script: deployment URL, triggers, Script Properties keys, linked spreadsheet, tab names, row-1 headers, and selected live formulas. | Access to live Google Apps Script and Google Sheet. | High: repo-only assumptions may not match production. | Inventory captured in docs without exposing secrets; discrepancies classified preserve/refactor/add/retire/unknown. | Manual read-only verification against live Apps Script and Sheet; no `setupSheet` run. | No if read-only; yes if accidentally running setup/mutation functions. |
| B-005 | `unknown` | Confirm Square webhook topology: direct Apps Script queue vs Cloudflare Worker D1 queue/pull. | Square Developer dashboard access, Worker environment, Apps Script Script Properties/triggers. | High: duplicate or missing webhook processing can create payment/order mismatch. | One production path designated primary; fallback path documented; test event and real low-dollar payment behavior recorded. | Square test webhook; real low-dollar payment; queue drain observation; idempotency check. | Yes, payment/order state may be touched during real-payment verification. |
| B-006 | `refactor` | Freeze sheet schema mutation by moving runtime header writes to explicit setup/migration documentation before code changes. | B-002 and production header inventory. | High: runtime header changes can corrupt live sheet assumptions. | No order submission path is expected to mutate row 1 except explicit migration/setup after approval. | Unit/static test for schema helper behavior after implementation; copied-sheet dry run. | Yes when implemented against production Sheets. |
| B-007 | `refactor` | Isolate one-time order persistence from payment-link generation failures. | B-004, B-005, Square credentials in staging/copy environment. | High: failed external payment APIs can prevent order rows from being written. | Orders persist with a recoverable payment-link status even when Square/Venmo/Cash link generation fails. | Apps Script dry-run tests on copied spreadsheet; mocked Square failure; successful normal order; owner recovery email check. | Yes, order rows and emails in production if deployed. |
| B-008 | `refactor` | Consolidate email failure tracking and resend diagnostics for order, subscription, owner-alert, newsletter, and review emails. | B-002, B-003, live Script Property inventory. | Medium: changing retries can duplicate customer emails. | Single documented queue/metadata convention; manual resend tooling shows all pending failures; idempotency rules documented. | Copied-sheet email test with MailApp disabled/mocked where possible; manual resend dry run; duplicate-send guard test. | Yes, can send emails and update retry metadata. |
| B-009 | `refactor` | Normalize email log tab naming (`Email Log` vs `Email Events`) and document retention/privacy masking. | B-002 and live workbook inventory. | Medium: log consumers may break or PII may be mishandled. | One canonical log tab name or compatibility strategy; masking policy documented; existing logs preserved. | Header comparison; sendTrackedEmail test on copied workbook; report query check. | Yes, email log rows may be written/migrated. |
| B-010 | `refactor` | Define newsletter/weekly drop operating model: audience source, unsubscribe handling, preview/approval, and dry-run mode. | B-003 and current email list inventory. | Medium: accidental bulk sends or missing opt-out handling. | Weekly drop cannot be sent without explicit approval/dry-run path; STOP/unsubscribe handling documented or implemented. | Dry-run send to owner; active/inactive recipient filtering test; unsubscribe/status update test. | Yes, affects email list and customer communications. |
| B-011 | `refactor` | Split Apps Script monolith conceptually into modules or documented sections before migration to Ratio services. | B-001, B-002, B-004. | Medium: large edits to production backend are risky. | Module map exists; each function has owner area and migration/retirement status. | Static call-site scan; Apps Script syntax check/clasp push to staging only when available. | No for documentation; yes for code implementation later. |
| B-012 | `retire` | Deprecate and later remove obsolete/duplicate helpers only after trigger/menu/deployment checks. Candidates include old direct Square receiver paths, overlapping trigger installers, and older capacity email helpers. | B-004 and B-005. | High: hidden trigger or manual menu dependency could break operations. | Candidate list has proof of no live trigger, menu item, URL, or documented manual procedure dependency; compatibility window completed. | Apps Script trigger inventory; call-site scan; staging run of affected workflows. | Potentially yes if removed from production script. |
| B-013 | `add` | Add Ratio Next.js App Router project skeleton under canonical `apps/ratio` namespace. | Approval to start app implementation; B-002 for data contracts. | Medium: can diverge from legacy behavior if contracts are incomplete. | Next.js + TypeScript project builds; package/import naming uses Ratio; no legacy root extension. | `npm install`; lint; typecheck; build; route smoke tests. | No initially. |
| B-014 | `add` | Add Ratio order API contract tests mirroring current order/subscription/delivery validation and totals. | B-002, B-013, current MENU and schedule rules. | High: validation drift can reject valid orders or accept bad ones. | Tests cover pickup Friday, delivery Thursday, cutoff, total mismatch, missing address, outside delivery, subscription Friday, and malformed/spam payloads. | Node test suite; fixtures comparing edge/API payloads to Apps Script expected responses. | No. |
| B-015 | `add` | Add inventory ledger/channel model for Ratio rather than relying only on derived sheet counts. | B-001, B-002, B-013. | High: inventory bugs affect overselling and fulfillment. | Ledger supports available, held, sold, cancelled/released, waitlisted, and reconciled states with audit trail. | Unit tests for state transitions; concurrency/duplicate submission tests; reconciliation dry run. | Yes when connected to production orders. |
| B-016 | `refactor` | Update GitHub Actions to validate both legacy safety checks and future Ratio checks. | B-013. | Low/Medium: CI can block emergency fixes if too strict. | Legacy validation remains; Ratio lint/typecheck/build/tests run when app exists; generated reports use correct paths. | GitHub Actions dry run or local equivalent commands. | No. |
| B-017 | `refactor` | Update maintenance/deployment scripts to use `rg` instead of recursive `grep` and remove hard-coded legacy assumptions as Ratio matures. | B-013 and deployment target decision. | Low: script mistakes can produce false CI failures. | Scripts pass locally; legacy endpoint checks remain while legacy production is active; Ratio checks added behind file existence gates. | `./scripts/validate-site.sh`; `./scripts/create-maintenance-report.sh`; shellcheck if available. | No. |
| B-018 | `unknown` | Verify `wrangler.jsonc` expectations and deployment target configuration. | Repository/deployment inventory. | Medium: CI or deployment can fail if config is absent/stale. | Config presence and purpose documented; validation script matches actual deployment setup. | `test -f wrangler.jsonc`; validation workflow/local script. | No. |
| B-019 | `preserve` | Keep current Hotplate-free state unless a future dependency explicitly introduces it. | None. | Low. | Repo search remains clean for `Hotplate`/`hotplate`, or any future reference is documented. | `rg -n "Hotplate|hotplate" .` | No. |
| B-020 | `preserve` | Preserve legacy root behavior until Ratio is production-ready; avoid extending root HTML except explicit safety/content fixes. | `AGENTS.md`; production migration plan. | Medium: accidental feature work in legacy root increases migration cost. | PR reviews reject new feature work under legacy root; changes cite explicit safety/content need. | Review changed files; CI legacy validation. | Yes only if legacy production files are changed/deployed. |

## Near-term rollout sequence

1. **Fulfillment messaging + Loaf Reserve implementation**
   - Deploy only after the standard repository validation passes.
   - Smoke test the public order and contact paths after deployment, including Thursday delivery, Friday pickup, Loaf Reserve Friday-only validation, confirmation email wording, and payment-link generation.
   - Do not combine this deploy with inventory Script Property changes.
2. **Existing preservation safeguards verified as additive**
   - Keep `INVENTORY_ROLLOUT_MODE=OFF` and `INVENTORY_KILL_SWITCH` available during validation and deployment.
   - Validate one-time order behavior and Loaf Reserve preservation against committed fixtures or a copied workbook before touching production data.
   - Confirm active subscriptions remain active and pending subscriptions remain pending after the regression checks.
3. **Focused pre-SHADOW subscription allocation correctness**
   - Correct source attribution, membership-week eligibility, specialty product mapping, and subscription allocation idempotency before SHADOW is considered.
   - Validate with committed synthetic fixtures only; do not mutate, rewrite, reorder, delete, or append existing production `Subscriptions` rows.
   - Leave inventory operationally OFF until a separate owner-approved rollout step.
4. **Owner approval before SHADOW**
   - Enable SHADOW only after the preceding implementation and preservation checks pass and the controlled inventory checklist below has owner initials for the required staging/copy-workbook checks.
   - In SHADOW, review mismatch reports and audits before any ENFORCE discussion.

## Controlled inventory production rollout readiness checklist

Before changing any production Script Properties or deploying Apps Script for the controlled inventory integration, complete and record all of the following with dates and approver initials:

- [ ] Staging workbook verified against the approved `Weekly Inventory`, `Inventory Reservations`, `Inventory Audit`, and `Inventory Shadow Mismatches` tab shapes.
- [ ] Production workbook backup created and restore path confirmed.
- [ ] Script Properties documented with placeholder values only: `INVENTORY_ROLLOUT_MODE`, `INVENTORY_KILL_SWITCH`, `INVENTORY_DRY_RUN`, `INVENTORY_PRODUCTION_SPREADSHEET_ID`, existing `SPREADSHEET_ID`/`SHEET_ID`, capacity limits, Square keys, and email configuration.
- [ ] OFF mode verified for one-time orders and Loaf Reserve flows.
- [ ] SHADOW mode verified without inventory writes.
- [ ] One-time order mismatch report reviewed.
- [ ] Subscription allocation mismatch report reviewed.
- [ ] Inventory totals reconciled against order and reservation records.
- [ ] Subscription and one-time totals reconciled together for each fulfillment week.
- [ ] Rollback procedure tested by setting `INVENTORY_KILL_SWITCH=true` or `INVENTORY_ROLLOUT_MODE=OFF` without code changes.
- [ ] ENFORCE approval recorded by the owner before setting `INVENTORY_ROLLOUT_MODE=ENFORCE`.
- [ ] Post-deployment smoke tests defined for OFF, SHADOW, ENFORCE, duplicate submission, insufficient inventory, active subscription allocation, and invalid subscription product mapping.
